import { createContext } from "react"

export const userContext = createContext({
    userName : 'user',
    // user: null,
    // setUser: () => {},
    // isAuthenticated: false,
    // setIsAuthenticated: () => {}
})